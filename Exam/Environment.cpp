#include "Environment.h"

namespace stochastic {
    /**
     * @brief Environment should be empty because the reactions decay into the environment
     */
    Environment::Environment() = default;

} // stochastic